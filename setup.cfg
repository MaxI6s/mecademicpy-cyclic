[flake8]
max-line-length = 100
extend-ignore =
    # Handled by black.
    E203,
    # The public facade deliberately uses the PascalCase names of the robot API.
    N802
exclude =
    .git,
    .venv,
    __pycache__,
    build,
    dist
